git add . 
git commit -m "first upload"
git push -u origin master

#ssh-keygen -t rsa 


git  clone /path/to/repository

git clone user@host:/path/to/repository

git clone  git@code.sohuno.com:calerwang/mailindex_backup.git
git add *
git commit -m
git commit -m "ff"
git push origin master 
git 
git config --global user.email "calerwang@sohu-inc.com"
git config --global user.name "Caler Wang"

git tag 1.0.0
git push --tags

git pull   #首先刷新本地以后在checkout
git checkout 1.0.0    #下载指定tag 版本


https://github.com/vim-scripts/Python-mode-klen#id3

